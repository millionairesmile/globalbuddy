specVersion: "v1beta"
serviceId: "menulist"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "menulist-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
